/**
 * Socket hooks index
 * Exports all socket-related hooks
 */
import { useSocket } from './useSocket';
import { useSocketEvent, useSocketEmit, useSocketRoom } from './useSocketEvent';

export {
  useSocket,
  useSocketEvent,
  useSocketEmit,
  useSocketRoom
};
